# ==============================================================================
# BACKEND REQUIREMENTS - Python 3.12 (SDCC)
# Pacchetti per API, server async, AWS SDK e scraping
#
# NOTA (fix riproducibilità): le versioni sono ora PINNATE con "==".
# Motivo: senza versione fissa, ogni "docker build" può scaricare una release
# diversa dei pacchetti, rendendo la build non riproducibile e potenzialmente
# rompendo l'app quando una dipendenza introduce breaking changes. Pinnare
# garantisce che l'immagine costruita oggi in locale sia identica a quella
# costruita domani su EC2 dalla pipeline CI/CD.
# ==============================================================================

fastapi==0.139.0          # framework REST asincrono
uvicorn[standard]==0.50.2 # server ASGI; [standard] aggiunge uvloop/httptools (performance) e websockets
python-multipart==0.0.20  # parsing multipart/form-data: necessario per l'upload immagini (UploadFile) dell'endpoint OCR
pydantic==2.13.4          # validazione modelli I/O (Pydantic v2)
boto3==1.43.40            # AWS SDK per Python (Comprehend, Textract, Bedrock, DynamoDB, S3)
requests==2.34.2          # client HTTP usato per l'API REST di Apify
