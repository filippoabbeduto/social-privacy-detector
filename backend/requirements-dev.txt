# ==============================================================================
# BACKEND DEV/TEST REQUIREMENTS (SDCC)
# Dipendenze usate SOLO per i test, NON incluse nell'immagine Docker di
# produzione (che installa il solo requirements.txt) per tenerla leggera.
# Uso:  cd backend && pip install -r requirements-dev.txt && python -m pytest
# ==============================================================================

pytest==9.1.1
httpx==0.28.1             # richiesto da fastapi.testclient.TestClient per i test HTTP
